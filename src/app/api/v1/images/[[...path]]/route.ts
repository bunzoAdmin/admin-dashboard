import { imagesProxy } from '@/lib/backendProxy';

export const runtime = 'nodejs';

export const GET = imagesProxy.GET;
export const POST = imagesProxy.POST;
export const PUT = imagesProxy.PUT;
export const PATCH = imagesProxy.PATCH;
export const DELETE = imagesProxy.DELETE;
